name: Build AUR Packages

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  actions: write
  contents: write

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.read.outputs.packages }}
    steps:
      - uses: actions/checkout@v4

      - name: Read package list
        id: read
        run: |
          packages=$(jq -R -s -c 'split("\n") | map(select(length > 0))' packages)
          echo "packages=$packages" >> $GITHUB_OUTPUT

  build:
    needs: setup
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
    strategy:
      matrix:
        package: ${{ fromJson(needs.setup.outputs.packages) }}
      fail-fast: false

    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel git sudo

      - name: Create build user
        run: |
          useradd -m builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

      - name: Bootstrap paru
        run: |
          su - builder -c "git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg --syncdeps --noconfirm"
          pacman -U /home/builder/paru-bin/*.pkg.tar.zst --noconfirm

      - name: Build package
        env:
          PACKAGER: Sergey Kanafyev <${{ github.event.pusher.email || 'sergeykanafyev@gmail.com' }}>
        run: |
          su - builder -c "
            export PACKAGER='${PACKAGER}'
            export COMPRESSZST='(zstd -c -T0 --ultra -20 -)'
            paru -S ${{ matrix.package }} --noconfirm --skipreview --noprogressbar --removemake
          "

      - name: Copy package to workspace
        run: |
          find /home/builder/.cache/paru/clone/${{ matrix.package }} -maxdepth 1 -name "*.pkg.tar.zst" ! -name "*-debug-*" -exec cp --verbose {} . \;

      - name: Encode colons in filenames for artifact upload
        run: |
          for f in *.pkg.tar.zst; do
            newf="${f//:/%3a}"
            [ "$f" != "$newf" ] && mv -v "$f" "$newf"
          done

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: pkg-${{ matrix.package }}
          path: "*.pkg.tar.zst"
          if-no-files-found: error

  release:
    needs: build
    if: always()
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm github-cli

      - name: Download all package artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: pkg-*
          merge-multiple: true
          path: repo/

      - name: Decode colons in filenames
        run: |
          cd repo
          for f in *.pkg.tar.zst; do
            newf="${f//%3a/:}"
            [ "$f" != "$newf" ] && mv -v "$f" "$newf"
          done

      - name: Create repository database and publish release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          cd repo
          shopt -s nullglob
          pkgs=(*.pkg.tar.zst)
          if [ ${#pkgs[@]} -eq 0 ]; then
            echo "No packages built successfully, skipping release"
            exit 0
          fi

          repo-add arch-linux-repository.db.tar.gz "${pkgs[@]}"
          cp arch-linux-repository.db.tar.gz arch-linux-repository.db
          cp arch-linux-repository.files.tar.gz arch-linux-repository.files

          gh release delete latest --repo ${{ github.repository }} --yes 2>/dev/null || true
          gh api --method DELETE "/repos/${{ github.repository }}/git/refs/tags/latest" 2>/dev/null || true

          gh release create latest * \
            --repo ${{ github.repository }} \
            --title "Latest Build" \
            --notes "$(cat <<EOF
          Arch Linux custom repository containing ${#pkgs[@]} packages.

          Add to /etc/pacman.conf:
          \`\`\`ini
          [arch-linux-repository]
          Server = https://github.com/$GITHUB_REPOSITORY/releases/download/latest
          \`\`\`
          EOF
          )"
