name: Build Arch Package

on:
  push:
    branches:
      - main
#  schedule:
#    - cron: '0 2 * * *'
#  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel git sudo

      - name: Create build user
        run: |
          useradd -m builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Clone AUR package
        run: |
          su - builder -c "git clone https://aur.archlinux.org/ttf-google-fonts-git.git"

      - run: |
          pwd
          ls -la

      - name: Build package
        run: |
          su - builder -c "cd ttf-google-fonts-git && makepkg --nodeps --noconfirm"

      - run: |
          pwd
          ls -la

#
#      - name: Upload package
#        uses: actions/upload-artifact@v4
#        with:
#          name: arch-package
#          path: '*.pkg.tar.zst'
